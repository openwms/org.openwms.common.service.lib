spring:
  application:
    name: openwms-services
  main:
    banner-mode: "off"

server:
  port: ${PORT:8761}

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0

security:
  basic:
    enabled: false