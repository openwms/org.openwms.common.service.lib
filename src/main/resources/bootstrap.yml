security:
  user:
    password: sa
  basic:
    enabled: false

spring:
  application:
    name: common-service

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/

server:
  port: 8082

