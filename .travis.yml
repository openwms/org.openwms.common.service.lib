language: java

jdk:
  - openjdk8

services:
  - docker

after_success:
  - docker build -t openwms/org.openwms.common.service:$TRAVIS_BUILD_NUMBER .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master